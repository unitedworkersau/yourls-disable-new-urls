# Temporarily Disable New URLs

A [YOURLS](https://yourls.org) plugin that blocks creation of new short URLs while keeping existing redirects working.

Define `YOURLS_DISABLE_NEW_URLS` as `false` in `user/config.php` to temporarily allow new URLs without deactivating the plugin.
